package com.mabaya.model.selenium;

public enum Browser {
    CHROME,
    FIREFOX
    //todo - if we want we can add more browsers to be supported we just have to add it here to this enum
    //todo and add a method to support driver manager for this browser
}
